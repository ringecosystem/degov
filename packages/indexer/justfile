set shell := ["bash", "-euo", "pipefail", "-c"]

default:
    @just --list

pnpm := "pnpm"

# Environment
install:
    {{pnpm}} install --filter @degov/indexer... --frozen-lockfile --ignore-scripts

build:
    {{pnpm}} run build

test:
    {{pnpm}} run test

test-unit:
    {{pnpm}} run test

test-accuracy:
    {{pnpm}} run test

test-integration:
    {{pnpm}} run test
